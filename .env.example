# Copy to .env (gitignored) and fill in. Loaded by bench.data_intraday via
# python-dotenv when a source needs credentials.

# WRDS — institutional millisecond TAQ via the wrds Python client.
# Used by configs/intraday_1s_wrds.yaml and configs/intraday_1m_wrds.yaml.
WRDS_USERNAME=
WRDS_PASSWORD=
